# 独立游戏开发日志 – 怪物系统

## 前期记录（无具体日期）

### 1. 处理伤害
- **问题**：伤害 GE 对怪物不起效果
- **解决**：GE 未设置伤害值 ✅

### 2. 实用的调试工具
- **需求**：蓝图函数库，封装格式化输出、屏幕打印、控制台日志为一个节点
- **解决**：已创建蓝图函数库函数 ✅

### 3. 怪物 Grunt 死亡逻辑
- **状态**：已完成 ✅

### 4. 怪物 Grunt 受伤硬直动画逻辑
- **问题演变**：
  - 受伤动画无法覆盖 → 怀疑松散标签未添加到 ASC 上
  - Tag 检测正常，但动画仍然无法打断
- **最终方案**：使用 GE 受击保护。受击保护期间免疫伤害；保护消失时才能减少生命值，减血触发 `TryPlayHurtAnimation()` 播放受击动画 ✅

---

## 2026-05-18

### 5. 两个怪物工具函数

#### 转向函数 `FaceTowardsTarget()`
- 用于主动或被动转向（2D 角色，需操作 Sprite）
- **优化方案**：将转向相关函数提取到蓝图函数库
  - `SetFacingDirection()` – 已完成 ✅
  - `GetFacingDirection()` – 已完成 ✅
  - `SetFaceTowardsTarget()` – 🚧 正在实现

#### 感知攻击来源函数 `GetHitDirection()`
- 当前用途：判断攻击者相对方向，使怪物受击时面对攻击者
- 未来扩展：根据攻击力度和招式方向决定击飞/击退/击倒
- **状态**：已创建函数框架 ✅

---

## 下一步计划
- [ ] 完成 `SetFaceTowardsTarget()` 并测试
- [ ] 将 `FaceTowardsTarget()` 整合到怪物行为树
- [ ] 扩展 `GetHitDirection()` 参数（力度、方向类型）

---

*最后更新：2026-05-18*

---

## 2026-05-19

### 6. 转向函数拆分与实现

#### 函数拆分
将 `SetFaceTowardsTarget()` 拆分为两个更具体的函数：
- `SetFaceTowardsTargetActor(APaperZDCharacter* Myself, AActor* TargetActor)`
- `SetFaceToLocation(APaperZDCharacter* Myself, FVector TargetLocation)`

#### 实现状态
| 函数 | 状态 | 备注 |
|------|------|------|
| `SetFaceTowardsTargetActor()` | ✅ 代码完成，测试中存在问题 | 详见下方问题追踪 |
| `SetFaceToLocation()` | ✅ 已实现，待测试 | 标记：进入后续计划 |
| `ReverseFacing(APaperZDCharacter* Myself)` | ✅ 已实现，待测试 | 作用：立即转身；标记：进入后续计划 |

### 7. 当前问题追踪

#### 🔴 问题1：`SetFaceTowardsTargetActor()` 第一次攻击时 Myself 参数无效
- **现象**：怪物第一次被攻击时，函数内部 debug 信息显示 `Myself` 无效；第二次及之后攻击则正常。
- **初步定位**：`TargetActor` 是在怪物的 **Actor 重叠事件**发生时设置的变量，怀疑是重叠事件的触发时机或变量传递有问题。
- **尝试解决**：需要让 GE（Gameplay Effect）携带发起者信息并传递给受击实体。
  - `AddInstigator` 是 C++ 函数，默认未暴露给蓝图 → 改用其他方法。
  - **蓝图替代方案**：使用 **Gameplay Event**，在攻击者蓝图中发送携带信息的事件。

#### 🔴 问题2：使用 Gameplay Event 后，转向仍然错误
- **现象**：每次受到攻击都会转向，但无法正确面对玩家的攻击方向。
- **原因**：尚未定位，可能是事件传递的数据不完整，或者转向函数内部计算逻辑有误。

---

## 明日计划（2026-05-20）

- [ ] 修复 `SetFaceTowardsTargetActor()` 的错误转向问题
- [ ] 完善 **Gameplay Event** 事件传递机制，替代简单粗暴的 `Apply Gameplay Effect to Target`
- [ ] 完成 `SetFaceToLocation()` 的测试
- [ ] 完成 `ReverseFacing()` 的测试

---

*最后更新：2026-05-19*

---

## 2026-05-20

### 8. 转向函数修复与攻击机制完善

#### `SetFaceTowardsTargetActor()` 反复转向问题
- **原因**：参数 `ActionValue` 在参与向量计算前未限制范围，导致归一化时出现错误
- **解决**：将 `ActionValue` 限制到 [-1, 1] 范围内后再参与计算 ✅

#### 攻击事件传递
- **GameplayEvent.Damage** 测试成功 ✅
- **当前效果**：怪物被攻击时能第一时间面向攻击者，并正确受到伤害

### 9. 玩家与怪物的碰撞交互
- **需求回顾**：玩家不攻击时可穿过怪物，攻击时能顶着怪物攻击
- **实现状态**：已试验成功 ✅（补充记录）

### 10. 待测试功能（暂未应用）
- `SetFaceToLocation()` – 等待合适场景
- `ReverseFacing()` – 等待合适场景

### 11. 攻击招式系统设计（扩展规划）

#### 招式效果类型
- 击飞
- 击退
- 吸附
- 对应力度参数

#### 数据资产方案
- 使用 **数据资产（Data Asset）** 全面细化攻击招式
- 每个招式包含：
  - 动画
  - 伤害值
  - 控制类型
  - 控制力
  - （后续可扩展）
- **完成项**：C++ 类 `SkillDataAsset` 已完成 ✅
- **配置项**：`ComboA` 招式已配置

---

## 明日计划（2026-05-21）

- [ ] `GA_Attack` 升级为使用数据资产调用资源
- [ ] 通过 `SetByCaller` 传递信息

## 近几天计划

- [ ] 完成击飞、击倒、吸附、击退四种攻击效果

---

*最后更新：2026-05-20*

---

## 2026-05-22

### 12. GA 读取数据资产与 SetByCaller 流程

#### 当前进展
- GA 已能成功读取数据资产（SkillDataAsset）中的内容 ✅

#### 需求目标
- 让 GE（Gameplay Effect）携带 GA 中的技能数据（如伤害值），通过 `SetByCaller` 传递

#### 完整流程设计
1. 在数据资产中设置伤害值
2. 在 GA 中为伤害值设置对应的 Tag
3. 在 GE 中将 `Health` 修改设置为 `SetByCaller`
4. 在 GA 中制作 GE Spec（Gameplay Effect Spec）并保存到玩家身上
5. 在玩家的 `AttackBox` 检测时，将 GE Spec 塞进 `GameplayEvent` 中（**此步存在潜在问题**）
6. 事件发送给怪物
7. 怪物接收到事件 → Break → 使用 `Instigator` 和 `Context Handle`
8. 怪物受击逻辑：接受事件，判断是否死亡，通过 `Context Handle` 应用 GE，面向攻击者，播放受伤动画

#### 遇到的问题
- **现象**：怪物播放了受伤动画，但没有造成伤害
- **原因**：将 GE Spec 直接“塞进” Event 的方式可能有问题

#### 解决方案（采用新思路）
- 不让 Spec 塞进 Event，而是让**怪物自己去拿**
- 通过 **BPI_Combat 接口**传递 `PendingDamageSpec`
- 新流程：
  1. 怪物收到 `GameplayEvent`
  2. 拿到 `Instigator`（玩家 Actor）
  3. 从玩家 Actor 读取 `PendingDamageSpec`
  4. 调用 `Apply Gameplay Effect Spec to Self`
- **结果**：成功 ✅

### 13. 后续优化（已完成）
- 将“面向攻击者”和“播放受伤动画”放入**属性监听**中
- 触发条件：生命值减少时执行
- 设计考虑：未来加入毒池等环境伤害时，需要分辨伤害类型，此优化已兼容 ✅

---

## 明日计划（2026-05-23）

- [ ] 完成击飞、击倒、吸附、击退四种攻击效果

## 近几天计划

- [ ] 画出击飞、击倒、吸附、击退四种攻击效果对应的动画素材

---

*最后更新：2026-05-22*

---

## 2026-05-23

### 14. 强制位移与 SetByCaller 向量传递研究

#### 强制位移实现方案探索
- **方案一：作为属性** → ❌ 不可行，强制位移不属于持久化属性
- **方案二：通过 GE 计算类执行** → ❌ 不可行，计算类职责清晰，仅服务于属性和 `SetByCaller` 的 float 值，不允许参与数值计算以外的操作

#### SetByCaller 向量读取问题
- **问题**：蓝图中没有直接读取 `SetByCaller` 值的节点，需要 C++ 实现
- **解决方案**：编写蓝图函数库辅助函数

#### 新增 C++ 辅助函数

| 函数 | 用途 | 状态 |
|------|------|------|
| `static float GetCustomSetByCallerMagnitude(const FGameplayEffectSpecHandle& SpecHandle, FGameplayTag Tag)` | 从 SpecHandle 读取 `SetByCaller` 中设置的 float 值 | ✅ 成功 |
| `static void AssignSetByCallerVector(UPARAM(ref) FGameplayEffectSpecHandle& SpecHandle, FVector Vector)` | 将向量值写入 `SetByCaller`，默认使用 `SetByCaller.Dir.X/Y/Z` 锁定 | ✅ 成功 |
| `static FVector GetCustomSetByCallerVector(const FGameplayEffectSpecHandle& SpecHandle, FGameplayTag XTag, FGameplayTag YTag, FGameplayTag ZTag)` | 从 SpecHandle 读取由三个自定义 Tag 组合的向量 | ✅ 成功 |
| `static FVector GetDefaultSetByCallerVector(const FGameplayEffectSpecHandle& SpecHandle)` | 使用默认 `SetByCaller.Dir.X/Y/Z` 读取向量 | ✅ 成功 |

#### 意义
- 以上函数是读取击退、吸附、击飞等效果数据的基础，现已可用

### 15. 进度总结
- 今日未能完成击退、击飞、击倒、吸附四种效果

---

## 明日计划（2026-05-24）

- [ ] 完成击飞、击倒、吸附、击退四种攻击效果

## 近几天计划

- [ ] 画出击飞、击倒、吸附、击退四种攻击效果对应的动画素材

---

*最后更新：2026-05-23*

---

## 2026-05-24

### 16. 战斗系统架构升级：从零散功能到完整数据驱动闭环

#### 思路历程回顾
- **从具体效果到抽象系统**：明确“击退、吸附、击飞、击倒”不应是独立的 GA/GE，而是受击者身上一个统一函数的不同参数。“数据驱动反应”是今日核心认知升级 ✅
- **数据传递深度探索**：深入理解 `SetByCaller` 机制，用 C++ 函数库封装力度向量和控制类型的传递，实现招式完整“物理意图”的标准化打包
- **攻受流程统一重构**：确立 `GameplayEvent` 为唯一伤害入口，通过通用函数库 `ProcessDamageEvent` 统一处理，属性与标签监听各司其职
- **配置与表现分离**：`SkillDataAsset` 数据资产让招式设计完全脱离逻辑代码，策划可在编辑器通过填写向量和选择枚举完成

#### 核心工作成果

##### 新增 C++ 函数库（ACTBlueprintFunctionLibrary - Combat 分类）
| 函数 | 用途 |
|------|------|
| `CreateDamageSpec` | 打包技能数据（伤害、控制向量、控制类型等） |
| `ApplyDamageToTarget` | 应用伤害到目标 |
| `ApplyHitReactionFromSpec` | 解析并执行物理位移（击退/吸附/击飞/击倒） |

##### 完善配置结构
- `FSkillSegment` 结构体现在包含备注和完整控制向量
- 所有攻击效果可在 `SkillDataAsset` 蓝图实例中可视化配置

##### 打通完整链路
- **玩家侧**：GA 从数据资产读取配置 → 调用 `CreateDamageSpec` 打包 → 存入角色变量 → 碰撞时用 `GameplayEvent` 发送给怪物
- **怪物侧**：`GameplayEvent` 回调接收 → `ProcessDamageEvent` 执行 → 读取 `SetByCaller` 数据 → 应用位移、播放动画、更新状态

##### 建立调试日志体系
- 在关键数据传递环节（写入、读取、应用）添加标准日志输出
- 快速定位了 `ControlType` 标签缺失等配置问题

#### 经验教训
- ⚠️ **所有 `SetByCaller` 依赖的 `GameplayTag` 必须先在本项目设置中创建**，否则写入/读取会静默失败，导致效果为 0

#### 待办记录
- **位移手感微调**：目前怪物弹射速度偏慢，已设计“距离驱动位移”加速方案（临时增大刹车值），后续集中调试
- **倒地系统**：框架已搭建（如添加 `State.Downed` 标签），待倒地/起身动画资源就位后接入，当前已临时注释以免影响其他功能

---

## 明日计划（2026-05-25）

- [ ] 为四种主要控制效果制作/配置对应的 PaperZD 动画表现：
  - 向远处击飞（`LaunchUp`）→ 腾空动画
  - 扫腿击倒（`Down`）→ 倒地动画
  - 被打跪地 → 可作为击倒变体或新增控制类型
  - 挑飞到空中 → 击飞起手式动画

---

*最后更新：2026-05-24*

---

## 2026-05-25

### 17. 受击动画资源制作

#### 今日完成
- 扫腿倒地动画 ✅
- 挑飞到空中动画 ✅

#### 说明
- 对应击倒效果（`Down`）和击飞效果（`LaunchUp`）的视觉表现
- 待后续整合到动画蓝图及控制效果逻辑中

---

## 明日计划（2026-05-26）

- [ ] 完成剩余两种动画（向远处击飞、被打跪地）
- [ ] 将已完成的动画接入受击反应系统

---

*最后更新：2026-05-25*

---

## 2026-05-26

### 18. 受击动画资源制作（续）

#### 今日完成
- 向远处击飞动画 ✅
- 被打跪地动画 ✅
- 倒地起身动画 ✅

#### 说明
- 至此，四种控制效果对应的基础动画素材已齐备：
  - 击飞（`LaunchUp`）→ 向远处击飞 / 挑飞到空中
  - 击倒（`Down`）→ 扫腿倒地 + 倒地起身
  - 新增跪地变体（可扩展为特殊控制类型）

---

## 明日计划（2026-05-27）

- [ ] 动画素材整理与分类
- [ ] 导入引擎（PaperZD）
- [ ] 接入受击反应系统，完成动画蓝图逻辑对接

---

*最后更新：2026-05-26*
