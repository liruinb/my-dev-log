# 独立游戏开发日志 – 怪物系统

## 前期记录（无具体日期）

### 1. 处理伤害
- **问题**：伤害 GE 对怪物不起效果
- **解决**：GE 未设置伤害值 ✅

### 2. 实用的调试工具
- **需求**：蓝图函数库，封装格式化输出、屏幕打印、控制台日志为一个节点
- **解决**：已创建蓝图函数库函数 ✅

### 3. 怪物 Grunt 死亡逻辑
- **状态**：已完成 ✅

### 4. 怪物 Grunt 受伤硬直动画逻辑
- **问题演变**：
  - 受伤动画无法覆盖 → 怀疑松散标签未添加到 ASC 上
  - Tag 检测正常，但动画仍然无法打断
- **最终方案**：使用 GE 受击保护。受击保护期间免疫伤害；保护消失时才能减少生命值，减血触发 `TryPlayHurtAnimation()` 播放受击动画 ✅

---

## 2026-05-18

### 5. 两个怪物工具函数

#### 转向函数 `FaceTowardsTarget()`
- 用于主动或被动转向（2D 角色，需操作 Sprite）
- **优化方案**：将转向相关函数提取到蓝图函数库
  - `SetFacingDirection()` – 已完成 ✅
  - `GetFacingDirection()` – 已完成 ✅
  - `SetFaceTowardsTarget()` – 🚧 正在实现

#### 感知攻击来源函数 `GetHitDirection()`
- 当前用途：判断攻击者相对方向，使怪物受击时面对攻击者
- 未来扩展：根据攻击力度和招式方向决定击飞/击退/击倒
- **状态**：已创建函数框架 ✅

---

## 下一步计划
- [ ] 完成 `SetFaceTowardsTarget()` 并测试
- [ ] 将 `FaceTowardsTarget()` 整合到怪物行为树
- [ ] 扩展 `GetHitDirection()` 参数（力度、方向类型）

---

*最后更新：2026-05-18*

---

## 2026-05-19

### 6. 转向函数拆分与实现

#### 函数拆分
将 `SetFaceTowardsTarget()` 拆分为两个更具体的函数：
- `SetFaceTowardsTargetActor(APaperZDCharacter* Myself, AActor* TargetActor)`
- `SetFaceToLocation(APaperZDCharacter* Myself, FVector TargetLocation)`

#### 实现状态
| 函数 | 状态 | 备注 |
|------|------|------|
| `SetFaceTowardsTargetActor()` | ✅ 代码完成，测试中存在问题 | 详见下方问题追踪 |
| `SetFaceToLocation()` | ✅ 已实现，待测试 | 标记：进入后续计划 |
| `ReverseFacing(APaperZDCharacter* Myself)` | ✅ 已实现，待测试 | 作用：立即转身；标记：进入后续计划 |

### 7. 当前问题追踪

#### 🔴 问题1：`SetFaceTowardsTargetActor()` 第一次攻击时 Myself 参数无效
- **现象**：怪物第一次被攻击时，函数内部 debug 信息显示 `Myself` 无效；第二次及之后攻击则正常。
- **初步定位**：`TargetActor` 是在怪物的 **Actor 重叠事件**发生时设置的变量，怀疑是重叠事件的触发时机或变量传递有问题。
- **尝试解决**：需要让 GE（Gameplay Effect）携带发起者信息并传递给受击实体。
  - `AddInstigator` 是 C++ 函数，默认未暴露给蓝图 → 改用其他方法。
  - **蓝图替代方案**：使用 **Gameplay Event**，在攻击者蓝图中发送携带信息的事件。

#### 🔴 问题2：使用 Gameplay Event 后，转向仍然错误
- **现象**：每次受到攻击都会转向，但无法正确面对玩家的攻击方向。
- **原因**：尚未定位，可能是事件传递的数据不完整，或者转向函数内部计算逻辑有误。

---

## 明日计划（2026-05-20）

- [ ] 修复 `SetFaceTowardsTargetActor()` 的错误转向问题
- [ ] 完善 **Gameplay Event** 事件传递机制，替代简单粗暴的 `Apply Gameplay Effect to Target`
- [ ] 完成 `SetFaceToLocation()` 的测试
- [ ] 完成 `ReverseFacing()` 的测试

---

*最后更新：2026-05-19*

---

## 2026-05-20

### 8. 转向函数修复与攻击机制完善

#### `SetFaceTowardsTargetActor()` 反复转向问题
- **原因**：参数 `ActionValue` 在参与向量计算前未限制范围，导致归一化时出现错误
- **解决**：将 `ActionValue` 限制到 [-1, 1] 范围内后再参与计算 ✅

#### 攻击事件传递
- **GameplayEvent.Damage** 测试成功 ✅
- **当前效果**：怪物被攻击时能第一时间面向攻击者，并正确受到伤害

### 9. 玩家与怪物的碰撞交互
- **需求回顾**：玩家不攻击时可穿过怪物，攻击时能顶着怪物攻击
- **实现状态**：已试验成功 ✅（补充记录）

### 10. 待测试功能（暂未应用）
- `SetFaceToLocation()` – 等待合适场景
- `ReverseFacing()` – 等待合适场景

### 11. 攻击招式系统设计（扩展规划）

#### 招式效果类型
- 击飞
- 击退
- 吸附
- 对应力度参数

#### 数据资产方案
- 使用 **数据资产（Data Asset）** 全面细化攻击招式
- 每个招式包含：
  - 动画
  - 伤害值
  - 控制类型
  - 控制力
  - （后续可扩展）
- **完成项**：C++ 类 `SkillDataAsset` 已完成 ✅
- **配置项**：`ComboA` 招式已配置

---

## 明日计划（2026-05-21）

- [ ] `GA_Attack` 升级为使用数据资产调用资源
- [ ] 通过 `SetByCaller` 传递信息

## 近几天计划

- [ ] 完成击飞、击倒、吸附、击退四种攻击效果

---

*最后更新：2026-05-20*

---

## 2026-05-22

### 12. GA 读取数据资产与 SetByCaller 流程

#### 当前进展
- GA 已能成功读取数据资产（SkillDataAsset）中的内容 ✅

#### 需求目标
- 让 GE（Gameplay Effect）携带 GA 中的技能数据（如伤害值），通过 `SetByCaller` 传递

#### 完整流程设计
1. 在数据资产中设置伤害值
2. 在 GA 中为伤害值设置对应的 Tag
3. 在 GE 中将 `Health` 修改设置为 `SetByCaller`
4. 在 GA 中制作 GE Spec（Gameplay Effect Spec）并保存到玩家身上
5. 在玩家的 `AttackBox` 检测时，将 GE Spec 塞进 `GameplayEvent` 中（**此步存在潜在问题**）
6. 事件发送给怪物
7. 怪物接收到事件 → Break → 使用 `Instigator` 和 `Context Handle`
8. 怪物受击逻辑：接受事件，判断是否死亡，通过 `Context Handle` 应用 GE，面向攻击者，播放受伤动画

#### 遇到的问题
- **现象**：怪物播放了受伤动画，但没有造成伤害
- **原因**：将 GE Spec 直接“塞进” Event 的方式可能有问题

#### 解决方案（采用新思路）
- 不让 Spec 塞进 Event，而是让**怪物自己去拿**
- 通过 **BPI_Combat 接口**传递 `PendingDamageSpec`
- 新流程：
  1. 怪物收到 `GameplayEvent`
  2. 拿到 `Instigator`（玩家 Actor）
  3. 从玩家 Actor 读取 `PendingDamageSpec`
  4. 调用 `Apply Gameplay Effect Spec to Self`
- **结果**：成功 ✅

### 13. 后续优化（已完成）
- 将“面向攻击者”和“播放受伤动画”放入**属性监听**中
- 触发条件：生命值减少时执行
- 设计考虑：未来加入毒池等环境伤害时，需要分辨伤害类型，此优化已兼容 ✅

---

## 明日计划（2026-05-23）

- [ ] 完成击飞、击倒、吸附、击退四种攻击效果

## 近几天计划

- [ ] 画出击飞、击倒、吸附、击退四种攻击效果对应的动画素材

---

*最后更新：2026-05-22*

---

## 2026-05-23

### 14. 强制位移与 SetByCaller 向量传递研究

#### 强制位移实现方案探索
- **方案一：作为属性** → ❌ 不可行，强制位移不属于持久化属性
- **方案二：通过 GE 计算类执行** → ❌ 不可行，计算类职责清晰，仅服务于属性和 `SetByCaller` 的 float 值，不允许参与数值计算以外的操作

#### SetByCaller 向量读取问题
- **问题**：蓝图中没有直接读取 `SetByCaller` 值的节点，需要 C++ 实现
- **解决方案**：编写蓝图函数库辅助函数

#### 新增 C++ 辅助函数

| 函数 | 用途 | 状态 |
|------|------|------|
| `static float GetCustomSetByCallerMagnitude(const FGameplayEffectSpecHandle& SpecHandle, FGameplayTag Tag)` | 从 SpecHandle 读取 `SetByCaller` 中设置的 float 值 | ✅ 成功 |
| `static void AssignSetByCallerVector(UPARAM(ref) FGameplayEffectSpecHandle& SpecHandle, FVector Vector)` | 将向量值写入 `SetByCaller`，默认使用 `SetByCaller.Dir.X/Y/Z` 锁定 | ✅ 成功 |
| `static FVector GetCustomSetByCallerVector(const FGameplayEffectSpecHandle& SpecHandle, FGameplayTag XTag, FGameplayTag YTag, FGameplayTag ZTag)` | 从 SpecHandle 读取由三个自定义 Tag 组合的向量 | ✅ 成功 |
| `static FVector GetDefaultSetByCallerVector(const FGameplayEffectSpecHandle& SpecHandle)` | 使用默认 `SetByCaller.Dir.X/Y/Z` 读取向量 | ✅ 成功 |

#### 意义
- 以上函数是读取击退、吸附、击飞等效果数据的基础，现已可用

### 15. 进度总结
- 今日未能完成击退、击飞、击倒、吸附四种效果

---

## 明日计划（2026-05-24）

- [ ] 完成击飞、击倒、吸附、击退四种攻击效果

## 近几天计划

- [ ] 画出击飞、击倒、吸附、击退四种攻击效果对应的动画素材

---

*最后更新：2026-05-23*

---

## 2026-05-24

### 16. 战斗系统架构升级：从零散功能到完整数据驱动闭环

#### 思路历程回顾
- **从具体效果到抽象系统**：明确“击退、吸附、击飞、击倒”不应是独立的 GA/GE，而是受击者身上一个统一函数的不同参数。“数据驱动反应”是今日核心认知升级 ✅
- **数据传递深度探索**：深入理解 `SetByCaller` 机制，用 C++ 函数库封装力度向量和控制类型的传递，实现招式完整“物理意图”的标准化打包
- **攻受流程统一重构**：确立 `GameplayEvent` 为唯一伤害入口，通过通用函数库 `ProcessDamageEvent` 统一处理，属性与标签监听各司其职
- **配置与表现分离**：`SkillDataAsset` 数据资产让招式设计完全脱离逻辑代码，策划可在编辑器通过填写向量和选择枚举完成

#### 核心工作成果

##### 新增 C++ 函数库（ACTBlueprintFunctionLibrary - Combat 分类）
| 函数 | 用途 |
|------|------|
| `CreateDamageSpec` | 打包技能数据（伤害、控制向量、控制类型等） |
| `ApplyDamageToTarget` | 应用伤害到目标 |
| `ApplyHitReactionFromSpec` | 解析并执行物理位移（击退/吸附/击飞/击倒） |

##### 完善配置结构
- `FSkillSegment` 结构体现在包含备注和完整控制向量
- 所有攻击效果可在 `SkillDataAsset` 蓝图实例中可视化配置

##### 打通完整链路
- **玩家侧**：GA 从数据资产读取配置 → 调用 `CreateDamageSpec` 打包 → 存入角色变量 → 碰撞时用 `GameplayEvent` 发送给怪物
- **怪物侧**：`GameplayEvent` 回调接收 → `ProcessDamageEvent` 执行 → 读取 `SetByCaller` 数据 → 应用位移、播放动画、更新状态

##### 建立调试日志体系
- 在关键数据传递环节（写入、读取、应用）添加标准日志输出
- 快速定位了 `ControlType` 标签缺失等配置问题

#### 经验教训
- ⚠️ **所有 `SetByCaller` 依赖的 `GameplayTag` 必须先在本项目设置中创建**，否则写入/读取会静默失败，导致效果为 0

#### 待办记录
- **位移手感微调**：目前怪物弹射速度偏慢，已设计“距离驱动位移”加速方案（临时增大刹车值），后续集中调试
- **倒地系统**：框架已搭建（如添加 `State.Downed` 标签），待倒地/起身动画资源就位后接入，当前已临时注释以免影响其他功能

---

## 明日计划（2026-05-25）

- [ ] 为四种主要控制效果制作/配置对应的 PaperZD 动画表现：
  - 向远处击飞（`LaunchUp`）→ 腾空动画
  - 扫腿击倒（`Down`）→ 倒地动画
  - 被打跪地 → 可作为击倒变体或新增控制类型
  - 挑飞到空中 → 击飞起手式动画

---

*最后更新：2026-05-24*

---

## 2026-05-25

### 17. 受击动画资源制作

#### 今日完成
- 扫腿倒地动画 ✅
- 挑飞到空中动画 ✅

#### 说明
- 对应击倒效果（`Down`）和击飞效果（`LaunchUp`）的视觉表现
- 待后续整合到动画蓝图及控制效果逻辑中

---

## 明日计划（2026-05-26）

- [ ] 完成剩余两种动画（向远处击飞、被打跪地）
- [ ] 将已完成的动画接入受击反应系统

---

*最后更新：2026-05-25*

---

## 2026-05-26

### 18. 受击动画资源制作（续）

#### 今日完成
- 向远处击飞动画 ✅
- 被打跪地动画 ✅
- 倒地起身动画 ✅

#### 说明
- 至此，四种控制效果对应的基础动画素材已齐备：
  - 击飞（`LaunchUp`）→ 向远处击飞 / 挑飞到空中
  - 击倒（`Down`）→ 扫腿倒地 + 倒地起身
  - 新增跪地变体（可扩展为特殊控制类型）

---

## 明日计划（2026-05-27）

- [ ] 动画素材整理与分类
- [ ] 导入引擎（PaperZD）
- [ ] 接入受击反应系统，完成动画蓝图逻辑对接

---

*最后更新：2026-05-26*

---

## 2026-05-27

### 19. 受击动画资源导入与蓝图连线

> 补充说明：昨日（2026-05-26）因工厂加班未能更新进度。之前的日期AI总结错误，今天才发现。从今天2026/5/27开始调整至正确日期。

#### 今日完成
- 以下动画已全部导入、分割，并与动画蓝图完成连线 ✅

| 动画用途 | 动画资源名 |
|---------|-----------|
| 受击硬直 | `PFB_Grunt_Hurt` |
| 死亡 | `PFB_Grunt_Death` |
| 趴下（面朝下） | `PFB_Grunt_Prone` |
| 趴下起身 | `PFB_Grunt_Prone_GetUp` |
| 躺倒（面朝上） | `PFB_Grunt_KnockDown` |
| 躺倒起身 | `PFB_Grunt_KnockDown_GetUp` |
| 绊腿倒地 | `PFB_Grunt_Trip` |
| 垂直击飞上升 | `PFB_Grunt_LaunchUp_Rise` |
| 垂直击飞空中过渡 | `PFB_Grunt_LaunchUp_Air` |
| 垂直击飞下降 | `PFB_Grunt_LaunchUp_Fall` |
| 垂直击飞落地 | `PFB_Grunt_LaunchUp_Land` |
| 远处击飞命中 | `PFB_Grunt_Knockback_Hit` |
| 远处击飞空中 | `PFB_Grunt_Knockback_Air` |
| 远处击飞落地 | `PFB_Grunt_Knockback_Land` |

---

## 明日计划（2026-05-28）

- [ ] 正式将上述动画接入受击逻辑（根据控制类型触发对应动画状态）

---

*最后更新：2026-05-27*

---

## 2026-05-28

### 20. 设计决策与死亡逻辑攻关

#### 设计决策
- **不做空战**：战斗以地面为主，空中仅用于移动和战略机动。空中受击累计伤害后直接坠落，参考《命运2》处理方式 ✅
- **大招系统规划**：定位为2-3分钟攒满的终结技，与双武术模组绑定。不同模组释放效果不同（长距=范围冲击波，短距=单体连打）。充能来源：攻击命中、完美闪避、连击、击杀 ✅

#### `EHitControlType` 枚举扩展
- 从5种扩展为 **8种**：无、硬直、击退、强击退、吸附、击飞升空、击倒趴下、击倒躺下 ✅
- `GetControlTypeFromSpec` 添加了范围校验，防止非法值 ✅

#### `ApplyHitReactionFromSpec` 重构
- 废弃每种控制类型单独 `switch-case` 的方案
- **统一公式**：`LaunchVelocity = DirFromInstigator * Impulse.X + (0, 0, Impulse.Z)`
  - `Impulse.X`：沿攻击方向速度（正=推开，负=吸附）
  - `Impulse.Z`：垂直速度（正=向上，负=向下）
- 位移行为完全由数据配置决定，天然支持相对位置的击飞方向 ✅

#### 死亡逻辑攻关：解决“死在半空”
- **问题**：怪物击飞过程中血量归零，直接在空中播放死亡动画并销毁
- **弯路尝试**：
  - Tag保护方案 → 存在通知遗漏风险
  - 简单地面检测 → 导致击飞第一帧就触发死亡
- **最终方案**：确立**三条件死亡判定**
  1. 生命值归零
  2. 物理上处于地面
  3. 受击状态已结束（无受击保护 Tag）
- 受击系统与死亡逻辑完全解耦，死亡只在动画尘埃落定后介入 ✅

#### 怪物动画蓝图完善
- 受击动画连线判断已完成 ✅
- 战斗中五连击对应的不同受击动画已能正常播放 ✅
- **现存问题**：受击动画播放正常，但无法与死亡逻辑正常配合（播放死亡动画或销毁时机仍有冲突）⚠️

---

## 明日计划（2026-05-29）

- [ ] 解决受击动画与死亡逻辑的配合问题（死亡条件触发后应正确中断受击动画并播放死亡动画）
- [ ] 实现“三条件死亡监视”逻辑，条件全部满足后触发 `ExecuteDeath`
- [ ] 将 `EHitControlType` 接入动画蓝图状态机，根据枚举触发对应受击动画（已完成连线判断，需确认与死亡逻辑的优先级）
- [ ] 细化 `State.HitReaction` 标签的挂载与移除时机

---

*最后更新：2026-05-28*

---

## 2026-05-31

> 补充说明：5月29日、30日因工厂加班未能进行项目开发。

### 21. 怪物基础 AI 搭建与受击动画初步完成

#### 受击动画接入与死亡逻辑调试
- 将之前导入的全部受击动画（硬直、击退、击飞四段、倒地、死亡等）接入动画蓝图状态机 ✅
- **解决“怪物死在半空”核心难题**：
  - 问题：击飞过程中血量归零，直接在空中播放死亡动画并销毁
  - 弯路尝试：Tag保护方案（通知遗漏风险）；简单地面检测（击飞第一帧就触发死亡）
  - 最终方案：**三条件死亡判定**——生命值归零 + 物理在地面 + 受击状态已结束
  - 实现方式：通过移动模式标签（`InSky`/`OnLand`）驱动，属性监听添加 `Dead` 标签，延迟 0.2 秒后检查 `OnLand` 决定立即死亡还是等待落地 ✅
- **调整击飞节奏**：增大 `Impulse.Z` 加快升空速度，利用 `InSky` 标签动态增大 `GravityScale` 加快落地 ✅

#### AI 三件套配置
- 创建并配置 `AIC_Grunt`（AIController）、`BB_Grunt`（黑板）、`BT_Grunt`（行为树），成功让怪物在场景中自主移动 ✅

#### 行为树结构
- 根节点 `Sequence`，装饰器 `State != Dead` 保证死亡后不执行任何行为
- `Selector` 下分**巡逻分支**和**战斗分支**（战斗分支留空，后续补充）
- 巡逻分支：`Wait`（随机 2-5 秒）→ `Find Random Location` → `Set MoveToLocation` → `Move To`

#### 移动锁定与朝向
- 将怪物旋转设为绝对旋转，锁定 Y 轴位移，完美限制在 XZ 平面内移动 ✅
- **朝向翻转**：在怪物 Tick 中读取 `Velocity.X`，大于阈值时执行 `SetFacingDirection`。函数内部对比 X 速度符号与当前 Sprite X 缩放，相同时跳过，不同时翻转，开销极低 ✅
- **朝向抖动修复**：设置速度死区，受击/死亡状态保护 ✅

#### 动画蓝图行走阈值
- 设置 `Speed > 10.0f` 才进入行走状态，低于阈值的微小滑动不再触发行走动画 ✅

#### 受控状态设计
- 确定方案：使用 GE 添加 `State.CC.xxx` 标签标记受控状态，起身动画最后一帧通过 `AnimNotify` 清除。行为树最高优先级检查失控 Tag，有则 `Wait` ✅
- 失控 GE 的具体实现留到明天

---

## 明日计划（2026-06-01）

- [ ] 实现失控 GE，在受击时应用，起身动画 `AnimNotify` 移除
- [ ] 行为树接入失控 Tag 检查，受控期间禁止 AI 行动
- [ ] 测试完整链路：受击 → 失控 → 动画 → 起身 → 恢复自由 → AI 继续行动

---

*最后更新：2026-05-31*

---

## 2026-06-01

### 22. 失控状态系统实现与 AI 联动

#### 引擎路线决策
- 结合未来目标风格（3D 背景 + 2D 手绘角色 + Lumen/Nanite 华丽光影），确认继续留在 **虚幻 5** 是最优解 ✅
- **对比分析**：
  - Godot：2D/3D 渲染管线分离，难以实现 2D 角色受 3D 光照效果
  - Unity：需要自建渲染管线，工作量大
  - UE5：Lumen + PaperZD 能天然融合这套视觉方案

#### 失控 GE 设计
- 确定通用失控 GE：`GE_Public_LostControl`，玩家与怪物共用 ✅
- **失控 Tag 简化**：单一 `State.LostControl`，不区分硬直/击退/击飞/倒地类型。动画表现由 `EHitControlType` 驱动，AI 行为只需知道“能否行动” ✅
- **持续时间**：设为无限，由起身动画最后一帧的 `AnimNotify` 手动清除，避免动画未播完 GE 自动过期 ✅

#### Tag 事件绑定与黑板同步
- 使用 `RegisterGameplayEventTag` 绑定 `State.LostControl` 的添加/移除事件，**零 Tick 开销** ✅
- 事件回调中通过 `GetController()` → `Cast to AIC_Grunt` → `Get Blackboard` 更新黑板变量 `bIsLostControl` ✅
- 确认运行时操作的是 `UBlackboardComponent` 实例，不需要 Cast 到 `BB_Grunt` 资产 ✅

#### 行为树接入失控分支
- 在行为树 `Selector` 最左侧添加失控分支：
  - `Sequence` 装饰器 `Blackboard "bIsLostControl == true"`
  - 装饰器 `Observer Aborts` 设为 `Both`，保证状态变化时立即重新评估
  - 子节点 `Wait`（无限等待），失控期间 AI 不执行任何动作
- **完整优先级**：失控 Wait → 战斗/追击（待补）→ 巡逻

#### 完整失控链路
  受击 → 应用 GE_Public_LostControl → Tag State.LostControl 添加
  → Tag 事件触发 → 黑板 bIsLostControl = true
  → 行为树进入失控 Wait 分支
  → 起身动画最后一帧 AnimNotify 清除 GE
  → Tag 移除 → 事件触发 → 黑板 bIsLostControl = false
  → 行为树恢复巡逻/追击

---

## 明日计划（2026-06-02）

- [ ] 添加感知组件（`AIPerceptionComponent`），实现怪物视觉感知玩家
- [ ] 行为树追击分支：`Move To TargetActor`
- [ ] 测试完整 AI 循环：巡逻 → 发现玩家 → 追击 → 失控 → 恢复 → 继续追击

---

*最后更新：2026-06-01*

---

## 2026-06-02 至 2026-06-08

### 项目暂停说明
- 因近期工厂持续加班，6月2日至6月8日完全无法进行任何项目开发。
- 原定于6月2日启动的 AI 感知组件配置、行为树追击分支等工作被迫推迟。

---

## 2026-06-09

### 23. 阵营系统重构与怪物战术 AI 框架搭建

#### 阵营方案切换：从 Team ID 到 Gameplay Tag
- **弃用原因**：`IGenericTeamAgentInterface` 在蓝图中存在严重暴露问题，无法直接调用 `SetGenericTeamId` 节点，经反复尝试后确认纯蓝图无法闭环，必须依赖额外 C++ 桥接。
- **最终方案**：改用 Gameplay Tag 实现敌我识别。为角色 ASC 直接添加松散标签 `Faction.Player`（玩家）和 `Faction.Enemy`（怪物），在 AI 感知事件中通过 `HasMatchingGameplayTag` 手动过滤敌人。
- **实施结果**：怪物已能正确识别玩家并触发追击行为，阵营区分完全基于 GAS 标签体系，与现有失控/死亡状态管理自然统一 ✅
- **旧代码处理**：之前为 Team ID 编写的 C++ 全局求解器 (`SetupGlobalTeamAttitudeSolver`) 和 `SetTeamIdForController` 等函数已全部注释，附上弃用日期和理由，保留在函数库中以备未来参考。

#### AI 感知与 Tag 联动
- 怪物 `AIC_Grunt` 的视觉感知改为“检测中立”（Detect Neutrals），由蓝图在 `OnTargetPerceptionUpdated` 事件中手动检查目标 ASC 是否拥有 `Faction.Player` 标签，确认后写入黑板 `TargetActor`。
- 玩家 Pawn 添加 `AIPerceptionStimuliSource` 组件，确保能被怪物感知系统发现 ✅

#### 附近敌人数量统计函数（C++ 函数库扩展）
为了支持战斗决策（根据玩家周围敌人数量决定是否靠近），在 `ACTBlueprintFunctionLibrary` 中新增两个 C++ 蓝图可调用函数：
- `CountNearbyFactionActors(AActor* Center, float Radius, FGameplayTag FactionTag, TSubclassOf<AActor> ActorClass)`  
  通用版本，可指定检测的 Actor 类型和阵营标签。
- `CountNearbyPaperZDFactionActors(AActor* Center, float Radius, FGameplayTag FactionTag)`  
  便捷版本，固定检测 `APaperZDCharacter` 类型，专门为当前项目战斗角色设计。

**实现要点**：
- 使用 `World->OverlapMultiByObjectType` 进行球体碰撞查询，性能稳定，适合每 0.3~0.5 秒调用。
- 过滤链：指定 ActorClass → `IAbilitySystemInterface` → ASC 有效 → `HasMatchingGameplayTag`。
- 函数元数据加入中文关键词（“统计 敌人 数量 周围”等）和 ToolTip 说明，方便从蓝图搜索。

**开发状态**：代码已编写并编译通过 ✅

#### 怪物战术决策框架设计
**新增黑板变量**（`BB_Grunt`）：
- `PlayerNearbyEnemyCount` (Int)：玩家周围同阵营敌人数量
- `bShouldChase` (Bool)：暂未使用，预留
- `ChargeMode` (Int)：追击模式随机数（0=直接冲，1=Z字试探）
- `ZigPoint1`, `ZigPoint2` (Vector)：Z字型移动的预生成随机偏移点

**行为树分支规划**（尚未连线，框架已确定）：
- 在 `TargetActor Is Set` 的战斗分支下，使用 Service 定期统计 `PlayerNearbyEnemyCount`。
- 决策 Selector：
  - 若 `PlayerNearbyEnemyCount >= 5` → 执行 `Wait`（暂时观望，未来可扩展远程攻击）。
  - 若 `PlayerNearbyEnemyCount < 5` → 进入随机追击子分支：
    - `ChargeMode == 0`：直接 `Move To TargetActor`（径直冲锋）
    - `ChargeMode == 1`：依次 `Move To ZigPoint1` → `Move To ZigPoint2` → `Move To TargetActor`（来回晃动突进）

**优先级保障**：失控分支（`bIsLostControl`）和死亡条件仍然最高，战术决策不会干扰受击保护和死亡处理。

#### 代码整理规范
- 所有新增 C++ 代码均补充了中英文注释，关键函数使用了 `meta` 标签增强蓝图可搜索性。
- 项目目录下维护了一个“已废弃代码”记录，方便追溯技术路线变更。

---

## 明日计划（2026-06-10）

- [ ] 创建 `BTS_CountNearbyEnemies` 服务蓝图，调用统计函数并更新黑板 `PlayerNearbyEnemyCount`
- [ ] 创建 `BTS_RandomizeChargeMode` 服务，随机生成追击模式和 Z 字偏移点并写入黑板
- [ ] 在行为树中正式搭建战术决策分支，完成两种追击模式的节点连线
- [ ] 运行测试：调整人数阈值（5）、碰撞半径、随机偏移范围和移动速度，优化追击手感

---

*最后更新：2026-06-09*

---

## 2026-06-10

### 24. 项目架构评估与清理，怪物状态机骨架搭建

#### 引擎与重构评估
- 结合项目当前规模（GAS战斗闭环、动画状态机、阵营系统已完成）和后续需求，确认**无需重构或更换引擎** ✅
- 决策总结：
  - 继续使用 Epic 编译好的二进制引擎（Launcher版），不使用源码版，避免不必要的维护成本。
  - 暂不升级引擎版本，优先完成核心玩法，未来再评估迁移。
  - 不推倒已有代码，而是通过**行为树状态机化**和**黑板清理**逐步消除混乱。

#### 项目清理（减负）
- **C++ 函数库清理**：从 `ACTBlueprintFunctionLibrary` 中彻底移除已弃用的 Team ID 相关函数（`SetupGlobalTeamAttitudeSolver`、`SetTeamIdForController` 等）及相关 include，消除遗留干扰 ✅
- **黑板资产清理**（`BB_Grunt`）：
  - 删除/注释不再使用的临时键：`bShouldRetreat`、`RetreatLocation`、`ChargeMode`、`ZigPoint1`、`ZigPoint2` 等。
  - 明确保留核心键：`TargetActor`、`CombatState`（新枚举）、`bIsLostControl`、`SiegeTargetLocation`、`RoamingTargetLocation`、`HomeLocation`、`ActiveCommandTag`、`PlayerNearbyEnemyCount`。
- **行为树旧分支禁用**：将原有零散的“撤退分支”、“人数≥5观望分支”、“随机追击分支”全部 Disable，作为历史参考保留，不再参与逻辑。

#### 怪物战斗状态枚举创建
- 新建枚举 `E_MonsterCombatState`，包含：`Patrol`、`Roaming`、`Sieging`、`Attacking`、`Fleeing`、`LostControl`、`Dead` ✅
- 在黑板中添加 `CombatState` 键（类型为该枚举，默认 `Patrol`），作为行为树状态驱动的单一入口。

#### 核心评估服务 `BTS_StateEvaluator` 开始搭建
- 创建 Service 蓝图，挂载于行为树根节点，执行间隔 0.2 秒。
- 初步评估逻辑（测试版）：
  - 若 `TargetActor` 无效 → 状态设为 `Patrol`。
  - 若 `bIsLostControl` 为真 → 设为 `LostControl`。
  - 否则临时硬编码为 `Sieging`（用于验证状态切换，等待接入指挥官席位请求）。
- 目的：让行为树立即以状态机模式运转，后续逐步丰富逻辑。

#### 行为树重构为状态驱动结构
- 主 `Selector` 下根据 `CombatState` 枚举创建对应分支：
  ```
  Selector
   ├─ LostControl → Wait (无限)
   ├─ Dead → （由根装饰器抑制）
   ├─ (预留 Command 分支)
   ├─ Fleeing → Wait (占位)
   ├─ Attacking → Wait (占位)
   ├─ Sieging → Wait / MoveTo (占位测试)
   ├─ Roaming → Wait / MoveTo (占位测试)
   └─ Patrol → 巡逻逻辑
  ```
- 所有装饰器均为 `CombatState == xxx`，确保状态变化时自动切换分支，不再依赖零散的条件装饰器。
- 测试验证：Service 更新 `CombatState` 后，行为树能立即跳转到对应分支并执行相应动作，状态机骨架生效 ✅

#### 当前成果与评估
- 项目经过清理后，黑板和行为树结构清晰度显著提升。
- AI 决策入口收敛到单一 Service，后续扩展只需修改评估逻辑，不需要再动行为树结构。
- 指挥官子系统、席位分配、攻击互斥等功能将在状态机骨架上逐步集成。

---

## 明日计划（2026-06-11）

- [ ] 完善 `BTS_StateEvaluator` 的评估逻辑，接入指挥官蓝图（`BP_CombatZoneManager`）的席位请求
- [ ] 在行为树中实现 `Sieging` 和 `Roaming` 状态的实际移动逻辑（到位保持 / 游走移动）
- [ ] 创建 `BTS_AttackMonitor` 服务并实现攻击令牌请求
- [ ] 将遗留的追逐、后撤等旧逻辑完全迁移到新状态机并删除 Disable 节点（可选）

---

*最后更新：2026-06-10*
